# DELIBERATION_FINAL_PLAN.md — 多专家评审纪要 + 落地稿 v1.0（待批准）

> 2026-08-10 · 评审对象：`REVIEW_DRAFT_INTEGRATED.md` v1.0
> 评审方式：六位领域专家视角，三轮辩论 + 一轮裁决；每个裁决附实证锚点（实码文件/行）
> 说明：本轮评审由小白龙以六位领域专家立场完成，意见基于实码与工程原则，立场冲突刻意保留并裁决。

---

## 〇、专家阵容

| 代号 | 领域 | 立场基调 |
|------|------|---------|
| E1 可靠性 | 幂等 · 状态机 · 存储 · 分布式语义 | 先兜底，再扩展 |
| E2 效率 | LLM 成本 · 延迟 · prompt cache · 模型路由 | 一切以测量为准 |
| E3 安全 | 注入面 · 沙箱 · trust 分层 · 数据脱敏 | 默认不安全，逐项举证 |
| E4 ML | DL/NN 落地 · ONNX · 蒸馏 · 评测门槛 | 不做端到端，只做确定性分类 |
| E5 产品 | BaiLongMa 初衷 · 意识循环 · 多智能体协作 · 陪伴感 | 延伸必须兑现初衷，不许长歪 |
| E6 工程 | Node→Rust 移植对齐 · 测试 · 构建 · 维护性 | 一行改动都要有回归锚 |

---

## 一、第 1 轮：立场陈述

**E1 可靠性**：综合稿方向正确，四层幂等已强是事实（F3/F9 + schema 部分唯一索引）。但我要先把 Q2/Q3 两条 SQL 语义钉死：UNIQUE 幂等键在「失败态先落、成功态后到」的翻转场景下，**INSERT OR IGNORE 会静默吞掉成功态**——这不是小瑕疵，是观测层自己会撒谎。观测撒谎比没观测更糟。裁决前必须先定终态覆盖语义。另：Deduper 进程内实现（F3）可以接受，但要在文档里写明「重启失忆」是有意为之还是已知缺口——它影响 P1 幂等修复的范围。

**E2 效率**：支持 P0 观测先行，且强烈支持 Q4 的 usage_raw 全量保留——归一化列会演进，raw 不会说谎。但我要加一条：M1 的 flusher 如果 30s/100 条批量，TTFT 指标必须在流路径**同步**记内存（这不难，别为了统一走队列把 TTFT 也延迟化）。另外 Q1 我立场明确：M1 不早，LLM 轮未接线反而是最好的落地时机——接线前埋点，接线当天就有第一份真实数据，而不是补课。

**E3 安全**：综合稿把 delegate 加固放 P1/P2 我不接受。F6 是全库唯一肉眼可见注入面，改动就两处（拼串改参数数组直启 + 保留 taskkill 进程树），测试三条。成本这么低、风险这么明确，没理由排在「测量」之后。安全不是阶段，是每个阶段的地基。**我要把它提到 P0。** 同时：埋点数据本身是新攻击面/新隐私面——brain_ui_events 的脱敏链必须复用到 llm_calls（模型名、prompt 片段、工具参数都可能含敏感信息）。

**E4 ML**：三层架构（规则→本地小模型→旗舰）我完全同意，这是唯一工程上负责任的路径。但我要泼冷水：**现在连 LLM 轮都没接线，谈蒸馏为时过早**。唯一现在该做的、零成本的，是让 conversations 表自然积累（它本来就在长），并在 schema 层面预留 label 列——别到 P4 才发现语料没有标注位。Q7 我主张：语料积累从现在开始（零成本），蒸馏工程 P4 启动，中间不新增任何依赖。

**E5 产品**：综合稿的初衷映射（六线→四词）我认可，但我要挑战一个隐含假设：**多智能体协作到底要不要现在就做？** 现状是 AI Collaborators 已有骨架但沙箱副本 LLM 轮未接线——协作是「演示级」。如果 P0-P4 全做对但协作永远不接线，那六线就是自嗨。我要求：M2 工具台账必须带 delegated_from 维度（Q9），这样协作一接线就有信任账本，而不是接线时再补观测。

**E6 工程**：作为移植对齐的守门人，我关心两件事。第一，M1 改 7 处文件，每处都要有对应的 Node 侧锚点或明确标注「Rust 新增」——防止草案里混入与 Node 语义不一致的「顺手改进」。第二，**Q6 的「人工介入硬通道」必须进 P0**：自动迭代可以晚，但「人工介入」这个逃生口不能晚于任何自动行为。哪怕 P4 才自动改码，P0 就该有一条「任何自动动作可一键回滚」的机制（其实 rescue 备份已近在咫尺，形式化一下即可）。

---

## 二、第 2 轮：交叉质询

**E1 质询 E2**：你说 TTFT 要同步记内存——那「同步」和「异步」两套路径，会不会出现同一请求 TTFT 记录在内存、usage 记录走队列，两条链路对不上账的情况？观测层自己先分裂，怎么保证一致？

**E2 质询 E1**：你死磕终态覆盖语义，但事实是沙箱副本 LLM 轮未接线（F4），llm_calls 表根本还没有真实写入者。为一个未上线的表设计过度复杂的翻转语义，是不是在给还不存在的流量做优化？

**E3 质询 E5**：你说协作是「演示级」所以要现在铺信任账本——但 trust 分层（用户→主 agent→协作者）你一个字没提。账本记了账，谁来授权协作者动手？没有授权边界，账本只是事后审计，不是事前控制。

**E5 质询 E3**：你把 delegate 提到 P0，我同意——但沙箱 M5 你坚持放 P2。delegate 是「主动调子进程」、沙箱是「关住子进程」，两个不是一套东西吗？为什么一个 P0 一个 P2？

**E4 质询 E6**：你说「人工介入硬通道进 P0」——具体形态是什么？是 config 开关？是环境变量？还是 UI 按钮？不定义形态的「硬通道」是口号。

**E6 质询 E4**：你说 conversations 表预留 label 列——Node 版 conversations 表是 24 表之一，加列要过幂等迁移（table_info 补列）。你确认过加列不会触发「老库补列」路径的兼容问题？你查过那列现在是不是真没有？

**E3 质询 E1**：Q2 的终态覆盖，你还没回答最坏情况：flusher 先 flush 了 attempt=1 错误态，然后 attempt=2 成功态来了，UPDATE 覆盖——但如果 attempt=2 也失败，再来 attempt=3 成功，中间覆盖两次，最终行保留的是哪次 attempt 的 error_stage？审计追溯会丢中间态，你接受吗？

**E2 质询 E4**：蒸馏语料从 conversations 来——但 conversations 存的是用户与主 agent 的对话，是「人机对话」语料，不是「代码开发规划」语料。你要蒸馏的是代码任务分类器，语料分布根本不对，你打算怎么解决分布偏移？

**E5 质询 E2**：你坚持「M1 是最好时机」——但 M1 验收清单里写着「真实调用 SQL 检查」，接线前根本没法做这条。验收清单自己都不自洽，你打算怎么验收？

---

## 三、第 3 轮：聚焦辩论（三个争议点）

### 争议点 A：观测层的 SQL 幂等语义（E1 × E3 × E2）

**E1 定案提议**：llm_calls 保留 UNIQUE(request_id)，但**写入语义从 INSERT OR IGNORE 改为 UPSERT（冲突时 UPDATE）**，终态字段（status/usage/error_stage）取后写覆盖；attempt 列保留最大值并作为终态的一部分。审计追溯中间态的需求由「错误分支单独写一条 error 明细」满足——即错误不覆盖，成功覆盖，error 永远追加。这样：成功终态不会丢（E1 的担忧）、失败历史可追溯（E3 的担忧）、实现仍是单条 SQL。

**E3 补充**：接受。但 upsert 的 WHERE 条件要钉死是「同 request_id」，不允许任何路径绕过 request_id 裸 UPDATE——防未来别的埋点人图省事按时间戳更新。

**E2 让步**：接受。你说得对，为一个会真实存在的表（M4 周报依赖它）定对语义不是过度设计。但要求：upsert 逻辑集中在 repositories 一个函数里，别散在 flusher 各处。

**裁决**：采纳 UPSERT 方案。llm_calls 语义 = 成功覆盖错误、错误追加、attempt 取 max。单函数实现 + 单测锁三条路径（失败→成功 / 失败→失败 / 成功后再来一次）。

### 争议点 B：工具台账的唯一键维度（E1 × E5）

**E1**：Q3 的问题真实存在——重试路径下同一 round 的合法工具调用会被 UNIQUE(request_id, round, tool_name) 的 IGNORE 误伤。**修复：键加 attempt 维度** → UNIQUE(request_id, round, attempt, tool_name)。这样重试后同一 round 的第二次调用完整保留，审计不丢。

**E5**：同意加 attempt。同时要求 delegated_from 列进 M2（不是 M3）：协作一接线就有账可查，成本归属按 agent 汇总。这一列现在空着也合法（主 agent 直调时为 NULL），不增加迁移成本。

**E1**：delegated_from 同意进 M2，但明确：它只解决「谁发起的」，不解决「谁授权的」——授权是 E3 的 trust 分层管，别混。

**裁决**：采纳。llm_tool_calls 唯一键 = (request_id, round, attempt, tool_name)；M2 带 delegated_from 列（NULL 合法）；授权边界归 P2 trust 分层，不在台账语义内。

### 争议点 C：M1 时机与验收自洽（E2 × E5 × E6）

**E5**：验收清单「真实调用 SQL 检查」在接线前不可行，这条要么删、要么改口径。

**E2**：改口径，不删。改为「接线后回访验收」——M1 交付门槛 = 编译 + 单测 + 静态 SQL 检查（建表语句与 upsert 逻辑人工核对）；接线日自动触发「真实调用 SQL 检查」作为回访项。

**E6**：可以。但加一条：M1 的单测里必须包含「未接 MetricsCollector 时 caller/retry 行为与现在完全一致」的回归测试——确保埋点零侵入性有测试证据，而不是口头承诺。

**裁决**：采纳。M1 验收 = 编译 + 单测（含零侵入回归）+ 静态 SQL 核对；「真实调用 SQL 检查」转为接线日回访项，写进 M4 前置条件。

---

## 四、第 4 轮：裁决与投票

| 议题 | 裁决 | 依据 |
|------|------|------|
| Q1 M1 时机 | **P0 先行**，接线前落地最佳 | E2+E5 共识；接线当天即有真实数据 |
| Q2 llm_calls 翻转语义 | **UPSERT 方案**（成功覆盖错误、错误追加、attempt 取 max） | 争议点 A |
| Q3 台账唯一键 | **加 attempt 维度** (request_id, round, attempt, tool_name) | 争议点 B |
| Q4 usage_raw | **全量保留 raw**，归一化列缺失时置 NULL | E2 立场 |
| Q5 唤醒顺序 | reminders 访问器+合并器 **P1**；成本闸门 P1 但依赖 M4 唤醒成本观测 | E1+E2：先有账再设闸 |
| Q6 人工介入硬通道 | **进 P0**（config 开关形态，形式化 rescue 回滚） | E6 立场，E4 要求定义形态 |
| Q7 DL/NN 启动 | 语料零成本积累**现在开始**（conversations 自然增长 + 预留 label 列）；蒸馏工程 P4 | E4 立场 |
| Q8 delegate 加固 | **提升至 P0**（改动 2 处 + 测试 3 条） | E3 立场 |
| Q9 delegated_from | **进 M2**，NULL 合法；授权边界归 P2 trust 分层 | 争议点 B |
| 附加：埋点脱敏 | llm_calls/llm_tool_calls 复用 brain_ui_events 脱敏链 | E3 质询 |
| 附加：TTFT 同步记 | 流路径同步记内存，usage 走队列；两条链路以 request_id 对齐 | E2+E1 对账 |
| 附加：zero-invasion | M1 单测含「未接采集器行为一致」回归 | 争议点 C |

**优先级投票结果**（六票）：P0 = M1 建表 → M2 台账 → M3 上下文 → M4 周报 + delegate 加固 + 人工介入硬通道（并列）；P1 = 唤醒闭环 + 幂等修复；P2 = 沙箱 M5 + trust 分层 + 参数 schema 校验；P3 = 效率调校；P4 = 自动迭代 + DL/NN。

---

## 五、落地稿（修订后的执行计划）

### 5.1 里程碑总表

| 里程碑 | 阶段 | 范围 | 验收门槛 | 依赖 |
|--------|------|------|---------|------|
| M1 | P0 | 建 3 表 + caller/retry 埋点 + flusher（UPSERT 语义） | 编译 / 单测（含零侵入回归、三条翻转路径）/ 静态 SQL 核对 | 无 |
| M1.5 | P0 | delegate 注入面加固（cmd 参数数组直启 + 保留进程树） | 回归 3 条：正常调用 / 含元字符参数 / 超时强杀 | 无 |
| M2 | P0 | 工具台账（键含 attempt、带 delegated_from）+ 熔断/round_limit 事件 | 单测：重试路径不误伤、delegated_from 为空合法 | M1 |
| M3 | P0 | injector 上下文统计（section 命中 + context_bytes） | 单测 + 与 M1 表 JOIN 可用 | M1 |
| M4 | P0 | 周报（六指标 + 阈值信号）+ 唤醒成本观测 | 接线日回访「真实调用 SQL 检查」通过 | M1-M3 + 接线 |
| P1-1 | P1 | 唤醒闭环：reminders 访问器 + 合并器 + 周窗口预算闸门 | 合并器风暴测试（N 触发器→1 次唤醒） | M4（成本观测） |
| P1-2 | P1 | 幂等修复：LLM 重试幂等键（M1 已打底）+ 工具执行防重放 | 故障注入：模拟 provider 完成但响应丢失 | M1/M2 |
| P2-1 | P2 | 沙箱 M5 | 沙箱逃逸测试套件 | P1 全 |
| P2-2 | P2 | trust 分层（用户→主 agent→协作者）+ 全工具参数 schema 校验 | 注入面复测 0 告警 | P1 |
| P3 | P3 | 基于周报：缓存友好化（relocate_sections 数据驱动）/ 模型路由 / token 预算 | 关键指标环比（cache_rate↑、cost/turn↓） | M4 |
| P4 | P4 | 自动迭代三件套（人工介入硬通道已在 P0 预置 + 快照回滚 + 收敛判据）→ DL/NN 蒸馏 | 收敛判据单测 / 蒸馏模型回归门槛 | P2 + 语料 |

### 5.2 关键修订对原方案的增量（评审增值）

1. **llm_calls 从 INSERT OR IGNORE 改为 UPSERT**（成功覆盖错误、错误追加、attempt 取 max）——原设计会在「失败态先落、成功态后到」时静默吞掉成功态，观测层会撒谎。
2. **工具台账唯一键加 attempt 维度**——原设计重试路径会误伤同一 round 的合法调用。
3. **delegate 加固从 P2 提到 P0**——全库唯一注入面，改动小收益明确，安全不排期。
4. **人工介入硬通道进 P0**——config 开关形态，任何自动动作可一键回滚（形式化 rescue）。
5. **M2 带 delegated_from**——协作一接线即有信任账本。
6. **埋点表复用脱敏链**——观测数据本身是新隐私面。
7. **TTFT 同步记 + usage 队列记，request_id 对齐**——两条链路不分裂。
8. **零侵入回归测试**——「埋点不改行为」有测试证据。
9. **语料零成本积累现在开始**——conversations 自然增长 + 预留 label 列，蒸馏 P4 启动。
10. **验收口径自洽**——「真实调用 SQL 检查」转接线日回访项。

### 5.3 风险登记册

| # | 风险 | 概率 | 影响 | 缓解 |
|---|------|------|------|------|
| R1 | 埋点拖慢 TTFT | 低 | 中 | 流路径同步记内存 <1ms + 基准测试；flusher 队列异步 |
| R2 | 观测库膨胀 | 中 | 低 | 明细 20000 行滚动淘汰 + 日聚合永久 |
| R3 | flusher 崩溃丢末批 | 低 | 低 | best-effort 语义明确，指标可重建不阻塞 |
| R4 | reminders 与 Node 旧数据格式不兼容 | 中 | 中 | 按 Node 语义实现 + 迁移测试（老库补列路径已实证） |
| R5 | M1 未接线验收不足 | 中 | 低 | 单测 + 接线日回访验收（M4 前置） |
| R6 | delegate 直启引入兼容差异 | 中 | 中 | 保留 taskkill 进程树逻辑；回归 3 条；Windows 真实进程测试 |
| R7 | 唤醒成本失控 | 中 | 高 | 成本闸门 + 周窗口预算（M4 先出成本观测） |
| R8 | 自动迭代误改码 | 低(受控后) | 高 | 三件套缺一不启动；P0 预置人工介入硬通道 |
| R9 | 蒸馏语料分布偏移（人机对话 vs 代码任务） | 高 | 中 | 标注策略：只蒸馏确定性子问题（意图/敏感/嵌入）；代码任务分类器以规则层标注为 ground truth 冷启动 |
| R10 | 埋点数据含敏感信息 | 中 | 中 | 复用 brain_ui_events 脱敏链，写入前 scrub |

### 5.4 待批准清单

请批准以下事项（可整体批或逐项批）：

1. **P0 范围与顺序**：M1 → M1.5(delegate 加固) → M2 → M3 → M4；M1/M1.5 可并行启动
2. **评审修订 10 条**（§5.2）并入实施，尤其 UPSERT 语义与 attempt 维度
3. **M1 实施授权**：按 DRAFT_M1_LLM_METRICS.md 改 7 处 + 新 2 文件（含本评审修订）
4. **M1.5 实施授权**：delegate 注入面加固（新增，原不在 M1 草案内）
5. **人工介入硬通道（P0）**：config 开关形态设计纳入 M1 同期
6. **语料积累（零成本）**：conversations 表预留 label 列，随 M1 建表迁移一并做

批准后按里程碑逐项实施、逐项验收；每项完成即汇报，不等待全批。
